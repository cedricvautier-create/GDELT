name: Mise à jour GDELT temps réel

on:
  schedule:
    - cron: '0 */6 * * *'
  workflow_dispatch: # Pour le lancer manuellement

permissions:
  contents: write
  
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout du dépôt
        uses: actions/checkout@v4

      - name: Installer Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Installer les dépendances
        run: pip install websockets pandas requests yfinance

      - name: Exécuter la collecte GDELT
        run: python collect_gdelt.py

      - name: Executer la collecte ReliefWeb (OCHA)
        run: python collect_reliefweb.py
        
      - name: Executer la collecte Climat et Environnement
        run: python collect_climate.py

      - name: Exécuter la collecte Marché (Yahoo Finance Cloud)
        run: python collecte_market.py

      - name: Sauvegarder les données sur le dépôt
        run: |
          git config --global user.name 'GitHub Action'
          git config --global user.email 'action@github.com'
          if [ -f gdelt_alerts.json ]; then git add gdelt_alerts.json; fi
          if [ -f market_data.json ]; then git add market_data.json; fi
          if [ -f reliefweb_alerts.json ]; then git add reliefweb_alerts.json; fi
          if [ -f gdelt_cache.csv ]; then git add gdelt_cache.csv; fi
          if [ -f climate_fires_cache.csv ]; then git add climate_fires_cache.csv; fi
          if [ -f climate_stress_cache.json ]; then git add climate_stress_cache.json; fi
          git commit -m "⚡ Mise à jour automatique GDELT" || exit 0
          git push
